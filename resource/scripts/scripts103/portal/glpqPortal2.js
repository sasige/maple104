function enter(pi) {
	/*var eim = pi.getPlayer().getEventInstance();
		if (eim.getProperty("pass3") == null){
			pi.playerMessage("要想继续前进，必须启动所有的祭坛。");
		}else{
			eim.setProperty("1")
			pi.playerMessage("第个冒险家通过了。")
		pi.warp(803000900)
		}
		*/
		pi.openNpc(9900000);
}