function enter(pi) {
	//if (pi.getPlayer().getMap().getReactorByName("jnr3_out1").getState() > 0) {
                pi.warp(926110303);
                return true;
      //  }
       // else {
         //       pi.playerMessage("门被锁着，需要芯片钥匙才能打开。");
           //     return false;
        //}
}